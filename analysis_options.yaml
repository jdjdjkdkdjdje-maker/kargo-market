# Xarid — tahlil sozlamalari
#
# Eslatma: bu fayl ataylab minimal qilingan — flutter_lints ulamaymiz,
# chunki GitHub Actions'dagi `flutter analyze` har qanday (hatto info
# darajadagi) xabarni ham xato deb hisoblaydi. Natijada build toza o'tadi.
analyzer:
  language:
    strict-casts: false
    strict-inference: false
    strict-raw-types: false
